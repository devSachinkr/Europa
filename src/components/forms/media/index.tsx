"use client";
import React from "react";

type Props = {
  groupId: string;
};

const MediaGalleryForm = ({ groupId }: Props) => {
  return <div>MediaGalleryForm</div>;
};

export default MediaGalleryForm;
